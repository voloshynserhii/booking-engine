import { Injectable } from '@nestjs/common';

@Injectable()
export class FileService {
  // File service logic (stub)
  // MVP → local storage is acceptable
  // Production → recommend S3 or compatible storage
  // NEVER rely on frontend for storage
}